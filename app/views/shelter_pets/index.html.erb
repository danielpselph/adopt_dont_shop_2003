<%= link_to "Add New Pet", "shelters/#{@shelter.id}/pets", method: :get %>

<% @shelter.pets.each do |pet| %>
    <section id="pets-<%= pet.id %>">
        <h3>Name: <%= pet.name %></h3>
        <img style="width:40vw" src="<%= pet.image %>">
        <h4>Age: <%= pet.age %></h4>
        <h4>Sex: <%= pet.sex %></h4>
        <h4>Description: <%= pet.description %></h4>
        <h4>Status: <%= pet.adoption_status %></h4>
    </section>
<% end %><br>